# _2. Using MASM directives, write an assembly language code segment to implement the following:_

```c
if (a > b)
    a = a - 1
else
    if (b >= c)
        b = b - 2;
    else
        if (c > d)
            c = c + d;
        else
            d = d / 2;
```
<hr>

**Resposta:**